# GitHub Packages registry configuration for @elevanaltd scope
# Constitutional mandate: CI_QUALITY_GATES (line 178) - must work in CI environment
@elevanaltd:registry=https://npm.pkg.github.com/
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
# Authentication token injected via environment variable
# CI: GitHub Actions provides GITHUB_TOKEN automatically via NODE_AUTH_TOKEN
# Local: Create Personal Access Token with read:packages scope at https://github.com/settings/tokens
