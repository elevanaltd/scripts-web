# GitHub Packages registry configuration for @elevanaltd scope
# Registry redirect: @elevanaltd packages come from GitHub Packages, not public npm
@elevanaltd:registry=https://npm.pkg.github.com/
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}

# Authentication architecture (3 environments):
# GitHub CI:   ${NODE_AUTH_TOKEN} from setup-node action (automatic)
# Vercel:      ${NODE_AUTH_TOKEN} from environment variable (configured in dashboard)
# Local Dev:   ${NODE_AUTH_TOKEN} from environment variable (set in ~/.zshrc)
#
# ${NODE_AUTH_TOKEN} is a placeholder substituted by npm at runtime
# NODE_AUTH_TOKEN environment variable must be set in all environments
