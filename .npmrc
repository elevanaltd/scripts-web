# GitHub Packages registry configuration for @elevanaltd scope
# Registry redirect: @elevanaltd packages come from GitHub Packages, not public npm
@elevanaltd:registry=https://npm.pkg.github.com/
//npm.pkg.github.com/:_authToken=${NPM_TOKEN}

# Authentication architecture (3 environments):
# Local Dev: ${NPM_TOKEN} resolved from global ~/.npmrc
# GitHub CI: ${NPM_TOKEN} resolved from GITHUB_TOKEN secret
# Vercel:    ${NPM_TOKEN} resolved from NPM_TOKEN environment variable
#
# ${NPM_TOKEN} is a placeholder substituted by npm at runtime - safe to commit
